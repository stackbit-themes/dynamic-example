import * as React from 'react';

interface FlowsListProps {}
export type FlowsListComponent = React.FC<FlowsListProps>;

const FlowsList: FlowsListComponent = (props) => {
    return <div>Flows here!</div>;
};

export default FlowsList;
